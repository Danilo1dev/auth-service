services:
  auth-service:
    build: ./
    container_name: auth-service
    restart: always
    ports:
      - "8080:8080"
    environment:
      - SERVER_PORT=8080
    networks:
      - microservices-network

networks:
  microservices-network:
    driver: bridge